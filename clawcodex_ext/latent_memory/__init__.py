"""Latent (passive) long-term memory extension for ClawCodex.

Contains the passive memory plugin (``plugin``, ``passive``) and the bundled
Mem0-compatible memory server (``server``), plus environment helpers.
"""
