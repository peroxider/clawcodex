/**
 * 01-coldopen · narrations
 * ─────────────────────────────────────────
 * B 站钩子章 · 3 步 · ~9s
 *
 * 节拍 = 口播稿的节奏,这一步同时是 step 推进的唯一信号源。
 * 每一行对应 chapters.ts 切换到 `step === N` 时屏幕上发生的变化。
 */
export const narrations: string[] = [
  // step 0 — 黑场单字弹出 + 时钟显示「你睡了」
  "你睡了,agent 在干活。",
  // step 1 — 时钟跳到清晨 + PR 自己开了
  "早上醒来,PR 自己开了。",
  // step 2 — 收束 · 品牌名 + 一句定性
  "这就是 ClawCodex DevMind。",
];