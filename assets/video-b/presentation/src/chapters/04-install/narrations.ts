/**
 * 04-install · 安装 + 完整面貌章(精简版)
 * ─────────────────────────────────────────
 * B 站节奏章 · 5 步 · ~20s
 *   step 0  章标 INSTALL. · "装它只要 1 行"
 *   step 1  4 平台一行命令(并排 2 终端)
 *   step 2  "想改源码:" 大字过渡
 *   step 3  源码 3 步 + version + clawcodex 启动
 *   step 4  outro · GitCode 超链接
 *
 * 行内命令(如 `curl ... | bash`)在 TSX 中作为终端视觉显示,
 * narration 只保留口语节奏。
 */
export const narrations: string[] = [
  "装它只要 1 行。",
  "macOS、Linux、WSL 一行;PowerShell 也一行。复制粘贴回车。",
  "想改源码:",
  "git clone、uv venv、uv pip install,装好以后 --version 验一下,直接 clawcodex 启动。",
  "现在去装一个 → gitcode.com/chadwweng/clawcodex。",
];