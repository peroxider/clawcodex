# Operator Hints for Issue #6 (F-68 Feature Gate)

Stop broad exploration. Implement the feature gate system directly as specified in `docs/feature_plan/06-ccb-benchmark/f-68-feature-gate.md`.

## Required deliverables

1. Create package `src/services/feature_gate/` with these files:
   - `__init__.py` — export `FeatureRegistry` singleton, `get_registry()`, `feature_gated`, `feature_gated_class`
   - `types.py` — `FeatureFlag` dataclass (name, default, deps, mutex_with, description)
   - `registry.py` — `FeatureRegistry` with `register`, `is_enabled`, `enable`, `disable`, `check_deps`, `check_mutex`
   - `decorators.py` — `@feature_gated(name, fallback=None)` function decorator and `feature_gated_class`
   - `config.py` — load/save `~/.clawcodex/features.json`
   - `cli.py` — add `--enable NAME` and `--disable NAME` args to the main CLI

2. Priority resolution (highest to lowest): CLI arg > env var `CLAWCODEX_FEATURE_*` > config file > default.

3. Add minimal tests under `tests/services/feature_gate/` covering:
   - register + is_enabled default
   - env override
   - config load/save
   - dependency and mutex checks
   - decorator fallback behavior

4. Run the configured test command and fix any failures.

## Anti-patterns to avoid

- Do not read every file in the repo; read only the design doc and a few analogous existing modules (e.g., `src/services/`) for patterns.
- Do not implement F-102 hooks or F-70 plugin integration; those are separate issues.
- Keep the first working version minimal; do not over-engineer persistence or CLI UI.

## Verification

After implementing, run:
```bash
python3 -m pytest tests/services/feature_gate/ -q
```
If that directory does not exist yet, create it with the tests above.
