"""Core upstream-sync mechanisms.

Provides vendor management, patch engines, change analysis, layer auditing,
and sync orchestration. All modules are business-agnostic and driven entirely
by upstream-sync.yaml configuration.
"""
