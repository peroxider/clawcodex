"""upstream-sync: Generic upstream code synchronization framework.

A configuration-driven, business-agnostic toolkit for managing fork/port
projects that need to track upstream code changes.

This package is intentionally a skeleton (framework-first). Concrete
implementations will be filled in subsequent development phases.
"""

__version__ = "0.1.0"
