diff --git a/transports/worker_state_uploader.py b/transports/worker_state_uploader.py
--- a/transports/worker_state_uploader.py	2026-06-18 10:10:16.775651 +0000
+++ b/transports/worker_state_uploader.py	2026-06-22 09:03:27.571146 +0000
@@ -80,9 +80,7 @@
         if self._closed:
             return
         self._pending = (
-            _coalesce_patches(self._pending, patch)
-            if self._pending is not None
-            else dict(patch)
+            _coalesce_patches(self._pending, patch) if self._pending is not None else dict(patch)
         )
         # Fire-and-forget drain. Requires a running event loop — see
         # module docstring "Calling convention".
