diff --git a/plugins/dependency.py b/plugins/dependency.py
--- a/plugins/dependency.py	2026-06-18 10:10:16.206381 +0000
+++ b/plugins/dependency.py	2026-06-22 09:03:23.448310 +0000
@@ -41,12 +41,14 @@
                 continue
             available = plugins[dep_name]
             if not version_satisfies(available.version, dep_version):
-                result.conflicts.append(DependencyConflict(
-                    plugin_name=name,
-                    dependency_name=dep_name,
-                    required_version=dep_version,
-                    available_version=available.version,
-                ))
+                result.conflicts.append(
+                    DependencyConflict(
+                        plugin_name=name,
+                        dependency_name=dep_name,
+                        required_version=dep_version,
+                        available_version=available.version,
+                    )
+                )
 
     order = topological_sort(plugins)
     if order is None:
