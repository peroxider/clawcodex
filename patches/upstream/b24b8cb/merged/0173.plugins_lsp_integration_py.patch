diff --git a/plugins/lsp_integration.py b/plugins/lsp_integration.py
--- a/plugins/lsp_integration.py	2026-06-18 10:10:16.216694 +0000
+++ b/plugins/lsp_integration.py	2026-06-25 15:09:03.000000 +0800
@@ -24,7 +24,7 @@
     column: int
     message: str
     severity: DiagnosticSeverity = DiagnosticSeverity.ERROR
-    source: str = ""
+    source: str = ''
     code: str | None = None
 
 
@@ -34,7 +34,7 @@
     command: str
     args: list[str] = field(default_factory=list)
     language_ids: list[str] = field(default_factory=list)
-    root_uri: str = ""
+    root_uri: str = ''
     initialization_options: dict[str, Any] = field(default_factory=dict)
 
 
@@ -52,18 +52,18 @@
 def wrap_lsp_server_as_plugin(
     config: LspServerConfig,
     *,
-    description: str = "",
+    description: str = '',
 ) -> LspPluginWrapper:
     manifest = PluginManifest(
-        name=f"lsp-{config.name}",
-        description=description or f"LSP server: {config.name}",
-        version="1.0.0",
+        name=f'lsp-{config.name}',
+        description=description or f'LSP server: {config.name}',
+        version='1.0.0',
     )
 
     plugin = LoadedPlugin(
         name=manifest.name,
         manifest=manifest,
-        source=f"lsp:{config.name}",
+        source=f'lsp:{config.name}',
         enabled=True,
     )
 
