diff --git a/providers/openai_provider.py b/providers/openai_provider.py
--- a/providers/openai_provider.py	2026-06-18 10:10:16.710459 +0000
+++ b/providers/openai_provider.py	2026-06-22 09:03:23.700471 +0000
@@ -15,9 +15,7 @@
 class OpenAIProvider(OpenAICompatibleProvider):
     """OpenAI provider using OpenAI SDK."""
 
-    def __init__(
-        self, api_key: str, base_url: Optional[str] = None, model: Optional[str] = None
-    ):
+    def __init__(self, api_key: str, base_url: Optional[str] = None, model: Optional[str] = None):
         """Initialize OpenAI provider.
 
         Args:
@@ -38,8 +36,10 @@
             kwargs["base_url"] = self.base_url
         # Support SSL verification bypass for corporate/internal endpoints
         import os
+
         if os.environ.get("CLAWCODEX_SSL_VERIFY", "").lower() in ("0", "false", "no"):
             import httpx
+
             kwargs["http_client"] = httpx.Client(verify=False)
         return OpenAI(**kwargs)
 
