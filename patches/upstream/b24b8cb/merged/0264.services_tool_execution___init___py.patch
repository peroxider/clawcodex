diff --git a/services/tool_execution/__init__.py b/services/tool_execution/__init__.py
--- a/services/tool_execution/__init__.py	2026-06-18 10:10:16.586008 +0000
+++ b/services/tool_execution/__init__.py	2026-06-23 04:05:40.772989 +0000
@@ -1,49 +1,81 @@
-"""Tool execution services — streaming executor, orchestrator, and tool hooks."""
-
-from __future__ import annotations
-
-from .tool_execution import (
-    MessageUpdateLazy,
-    classify_tool_error,
-    run_tool_use,
-)
-from .tool_result_persistence import (
-    DEFAULT_MAX_RESULT_SIZE_CHARS,
-    PERSISTED_OUTPUT_CLOSING_TAG,
-    PERSISTED_OUTPUT_TAG,
-    PREVIEW_SIZE_BYTES,
-    PersistedToolResult,
-    PersistResult,
-    PersistToolResultError,
-    build_large_tool_result_message,
-    generate_preview,
-    get_persistence_threshold,
-    is_persist_error,
-    is_tool_result_content_empty,
-    maybe_persist_large_tool_result,
-    persist_tool_result,
-    process_tool_result_block,
-    resolve_tool_results_dir,
-)
-
-__all__ = [
-    "DEFAULT_MAX_RESULT_SIZE_CHARS",
-    "MessageUpdateLazy",
-    "PERSISTED_OUTPUT_CLOSING_TAG",
-    "PERSISTED_OUTPUT_TAG",
-    "PREVIEW_SIZE_BYTES",
-    "PersistResult",
-    "PersistToolResultError",
-    "PersistedToolResult",
-    "build_large_tool_result_message",
-    "classify_tool_error",
-    "generate_preview",
-    "get_persistence_threshold",
-    "is_persist_error",
-    "is_tool_result_content_empty",
-    "maybe_persist_large_tool_result",
-    "persist_tool_result",
-    "process_tool_result_block",
-    "resolve_tool_results_dir",
-    "run_tool_use",
-]
+"""Facade — src/services/tool_execution/ has been moved to clawcodex_ext.
+
+The full implementation now lives in
+:mod:`clawcodex_ext.services.tool_execution`. This module re-exports the
+public surface so existing ``from src.services.tool_execution import ...``
+call sites keep working without modification.
+"""
+
+from __future__ import annotations
+
+from clawcodex_ext.services.tool_execution import (
+    BASH_TOOL_NAME,
+    DEFAULT_MAX_RESULT_SIZE_CHARS,
+    MessageUpdate,
+    MessageUpdateLazy,
+    OrchestratorMessageUpdate,
+    PERSISTED_OUTPUT_CLOSING_TAG,
+    PERSISTED_OUTPUT_TAG,
+    PREVIEW_SIZE_BYTES,
+    PersistResult,
+    PersistToolResultError,
+    PersistedToolResult,
+    PreToolUseResult,
+    StreamingToolExecutor,
+    ToolUseBlock,
+    TrackedTool,
+    build_large_tool_result_message,
+    classify_concurrency_safe,
+    classify_tool_error,
+    generate_preview,
+    get_persistence_threshold,
+    is_persist_error,
+    is_tool_result_content_empty,
+    maybe_persist_large_tool_result,
+    partition_tool_calls,
+    persist_tool_result,
+    process_tool_result_block,
+    resolve_hook_permission_decision,
+    resolve_tool_results_dir,
+    run_post_tool_use_failure_hooks,
+    run_post_tool_use_hooks,
+    run_pre_tool_use_hooks,
+    run_tool_use,
+    run_tools,
+)
+
+__all__ = [
+    "BASH_TOOL_NAME",
+    "DEFAULT_MAX_RESULT_SIZE_CHARS",
+    "MessageUpdate",
+    "MessageUpdateLazy",
+    "OrchestratorMessageUpdate",
+    "PERSISTED_OUTPUT_CLOSING_TAG",
+    "PERSISTED_OUTPUT_TAG",
+    "PREVIEW_SIZE_BYTES",
+    "PersistResult",
+    "PersistToolResultError",
+    "PersistedToolResult",
+    "PreToolUseResult",
+    "StreamingToolExecutor",
+    "ToolUseBlock",
+    "TrackedTool",
+    "build_large_tool_result_message",
+    "classify_concurrency_safe",
+    "classify_tool_error",
+    "generate_preview",
+    "get_persistence_threshold",
+    "is_persist_error",
+    "is_tool_result_content_empty",
+    "maybe_persist_large_tool_result",
+    "partition_tool_calls",
+    "persist_tool_result",
+    "process_tool_result_block",
+    "resolve_hook_permission_decision",
+    "resolve_tool_results_dir",
+    "run_post_tool_use_failure_hooks",
+    "run_post_tool_use_hooks",
+    "run_pre_tool_use_hooks",
+    "run_tool_use",
+    "run_tools",
+]
