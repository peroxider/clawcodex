diff --git a/settings/managed_path.py b/settings/managed_path.py
--- a/settings/managed_path.py	2026-06-18 10:10:16.650598 +0000
+++ b/settings/managed_path.py	2026-06-25 15:09:03.000000 +0800
@@ -16,7 +16,7 @@
     1. CLAUDE_MANAGED_SETTINGS_PATH env var
     2. Platform-specific default paths
     """
-    env_path = os.environ.get("CLAUDE_MANAGED_SETTINGS_PATH")
+    env_path = os.environ.get('CLAUDE_MANAGED_SETTINGS_PATH')
     if env_path:
         p = Path(env_path)
         if p.exists():
@@ -24,21 +24,22 @@
 
     # Platform-specific defaults
     import platform
+
     system = platform.system()
 
-    if system == "Darwin":
-        managed = Path("/Library/Application Support/ClawCodex/managed-settings.json")
+    if system == 'Darwin':
+        managed = Path('/Library/Application Support/ClawCodex/managed-settings.json')
         if managed.exists():
             return managed
 
-    if system == "Linux":
-        managed = Path("/etc/clawcodex/managed-settings.json")
+    if system == 'Linux':
+        managed = Path('/etc/clawcodex/managed-settings.json')
         if managed.exists():
             return managed
 
-    if system == "Windows":
-        program_data = os.environ.get("ProgramData", r"C:\ProgramData")
-        managed = Path(program_data) / "ClawCodex" / "managed-settings.json"
+    if system == 'Windows':
+        program_data = os.environ.get('ProgramData', r'C:\ProgramData')
+        managed = Path(program_data) / 'ClawCodex' / 'managed-settings.json'
         if managed.exists():
             return managed
 
