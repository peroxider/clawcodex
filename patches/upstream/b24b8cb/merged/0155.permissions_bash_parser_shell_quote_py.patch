diff --git a/permissions/bash_parser/shell_quote.py b/permissions/bash_parser/shell_quote.py
--- a/permissions/bash_parser/shell_quote.py	2026-06-18 10:10:16.881073 +0000
+++ b/permissions/bash_parser/shell_quote.py	2026-06-25 15:09:03.000000 +0800
@@ -1,30 +1,3 @@
-from __future__ import annotations
+"""Facade — permissions/bash_parser/shell_quote.py moved to clawcodex_ext/permissions/bash_parser/."""
 
-import re
-import shlex
-
-
-def quote(s: str) -> str:
-    if not s:
-        return "''"
-    if re.match(r"^[a-zA-Z0-9._/=:@%^,+-]+$", s):
-        return s
-    return shlex.quote(s)
-
-
-def split_command(command: str) -> list[str]:
-    try:
-        return shlex.split(command)
-    except ValueError:
-        return [command]
-
-
-def is_glob_pattern(s: str) -> bool:
-    return bool(re.search(r"(?<!\\)[*?[\]]", s))
-
-
-def expand_home(path: str) -> str:
-    if path.startswith("~/") or path == "~":
-        import os
-        return os.path.expanduser(path)
-    return path
+from clawcodex_ext.permissions.bash_parser.shell_quote import *  # noqa: F401,F403
