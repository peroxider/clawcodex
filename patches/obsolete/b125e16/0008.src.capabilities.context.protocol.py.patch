diff --git a/src/capabilities/context_protocol.py b/src/capabilities/context_protocol.py
new file mode 100644
index 0000000..e33276a
--- /dev/null
+++ b/src/capabilities/context_protocol.py
@@ -0,0 +1,29 @@
+"""ContextBuilder Protocol — interface for context/prompt construction.
+
+This Protocol defines the contract for building execution context.
+Concrete implementation is in src/context_system/builder.py (build_context_prompt).
+
+See: src/tool_system/agent_loop.py imports from context_system
+"""
+
+from __future__ import annotations
+
+from pathlib import Path
+from typing import Protocol
+
+__all__ = ["ContextBuilderProtocol"]
+
+
+class ContextBuilderProtocol(Protocol):
+    """Protocol for building tool execution context.
+
+    Implementors must provide:
+      - build_context_prompt(workspace_root, *, cwd) -> str
+    """
+
+    def build_context_prompt(
+        self,
+        workspace_root: str | Path,
+        *,
+        cwd: str | Path | None = None,
+    ) -> str: ...  # pragma: no cover
\ No newline at end of file
